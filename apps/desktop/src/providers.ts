import { createHash, randomUUID } from 'node:crypto'; import { access, mkdtemp, realpath, rm, stat } from 'node:fs/promises'; import { tmpdir } from 'node:os'; import { basename, join, resolve } from 'node:path'; import { spawn } from 'node:child_process'; import { app } from 'electron';
export interface DesktopSource { location: string; } export interface ProviderResult { token: string; path: string; mime: 'audio/wav' | 'audio/mpeg'; durationSeconds: number; }
const mimeFor = (path: string): ProviderResult['mime'] => path.toLowerCase().endsWith('.wav') ? 'audio/wav' : path.toLowerCase().endsWith('.mp3') ? 'audio/mpeg' : (() => { throw new Error('Only WAV and MP3 are supported'); })();
async function tool(name: 'ffprobe' | 'yt-dlp' | 'ffmpeg') { const candidates = [process.env[`AMBIENTALHEEHEE_${name.replace('-', '').toUpperCase()}`], name, `/opt/homebrew/bin/${name}`, `/usr/local/bin/${name}`].filter(Boolean) as string[]; for (const candidate of candidates) try { await access(candidate); return candidate; } catch {} throw Object.assign(new Error(`Required tool missing: ${name}`), { code: 'TOOL_MISSING' }); }
async function run(file: string, args: string[], signal?: AbortSignal) { return await new Promise<string>((resolveRun, reject) => { const child = spawn(file, args, { shell: false, detached: true }); let stdout = ''; let stderr = ''; child.stdout.on('data', (value) => { stdout += value; }); child.stderr.on('data', (value) => { stderr += value; }); const abort = () => { process.kill(-child.pid!, 'SIGTERM'); }; signal?.addEventListener('abort', abort, { once: true }); child.once('error', reject); child.once('close', (code) => { signal?.removeEventListener('abort', abort); code === 0 ? resolveRun(stdout) : reject(new Error(stderr.slice(0, 8192))); }); }); }
async function probe(path: string, signal?: AbortSignal) { const ffprobe = await tool('ffprobe'); const output = await run(ffprobe, ['-v', 'error', '-show_entries', 'format=duration', '-of', 'json', path], signal); const duration = JSON.parse(output).format.duration; if (!Number.isFinite(Number(duration)) || Number(duration) <= 0) throw new Error('Invalid media metadata'); return Number(duration); }
export class DesktopProviderRegistry { private readonly grants = new Map<string, ProviderResult>(); async resolve(source: DesktopSource, scenarioDirectory: string, signal?: AbortSignal): Promise<ProviderResult> { if (/^https:\/\//.test(source.location)) return this.youtube(source.location, signal); const root = resolve(scenarioDirectory); const path = await realpath(resolve(root, source.location)); if (!path.startsWith(`${root}/`) && path !== root) throw new Error('Local source escapes audio root'); const info = await stat(path); if (!info.isFile()) throw new Error('Local source is not a regular file'); const result = { token: randomUUID(), path, mime: mimeFor(path), durationSeconds: await probe(path, signal) }; this.grants.set(result.token, result); return result; } get(token: string) { return this.grants.get(token); } revokeAll() { this.grants.clear(); } private async youtube(url: string, signal?: AbortSignal): Promise<ProviderResult> { if (!/^https:\/\/(?:www\.)?(?:youtube\.com\/(?:watch\?v=[^&]+|shorts\/[^?]+|embed\/[^?]+)|youtu\.be\/[^?]+)$/.test(url)) throw new Error('Unsupported YouTube URL'); const ytdlp = await tool('yt-dlp'), ffmpeg = await tool('ffmpeg'); const temp = await mkdtemp(join(tmpdir(), 'ambiental-')); const output = join(temp, 'audio.mp3'); try { await run(ytdlp, ['--no-playlist', '-x', '--audio-format', 'mp3', '--ffmpeg-location', ffmpeg, '-o', output, url], signal); const path = await realpath(output); const result = { token: createHash('sha256').update(path).digest('hex'), path, mime: 'audio/mpeg' as const, durationSeconds: await probe(path, signal) }; this.grants.set(result.token, result); return result; } catch (error) { await rm(temp, { recursive: true, force: true }); throw error; } } }
