import { describe, expect, it } from 'vitest'; import { inspectDataUri, parseScenario } from './schema.js'; import { IncreasingGaussianSchedule } from './scheduler.js';
const wav = 'data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAQB8AAIA+AAACABAAZGF0YQAAAAA=';
describe('scenario contract', () => { it('accepts the inline-only web scenario', () => expect(parseScenario(JSON.stringify({ version: 1, name: 'x', bases: [{ id: 'base', source: { location: wav } }], jumpers: [] }), 'web').bases).toHaveLength(1)); it('rejects local web locations', () => expect(() => parseScenario(JSON.stringify({ version: 1, name: 'x', bases: [{ id: 'base', source: { location: 'a.wav' } }], jumpers: [] }), 'web')).toThrow('Source not allowed')); it('rejects noncanonical data URI', () => expect(() => inspectDataUri('data:audio/wav;base64,AA')).toThrow('canonical')); }); describe('increasing gaussian schedule', () => { it('returns mean for zero deviation', () => expect(new IncreasingGaussianSchedule(15, 0).nextDelay({ next: () => 0.5 })).toBe(15000)); });
